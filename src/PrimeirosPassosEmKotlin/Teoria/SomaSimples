-> Desafio:
   Leia dois valores inteiros identificados como variáveis A e B.
   Calcule a soma entre elas e chame essa variável de SOMA.
   A seguir escreva o valor desta variável.

-> Entrada:
   . Leitura de duas variáveis, A e B, do tipo inteiro.

-> Lógica:
   . Crie uma variável, SOMA, que calcula a soma entre A e B.

-> Saída:
   . Escreva a saída da variável SOMA.

-> Exemplos:
    1) A = 30, B = 10, SOMA = 40
    2) A = -30, B = 10, SOMA = -20
    3) A = 0, B = 0, SOMA = 0